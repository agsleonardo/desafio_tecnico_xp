// const sinon = require('sinon');
const chai = require('chai');

const { expect } = chai;
const { describe, it } = require('mocha');

describe('Testes do microserviço Accounts', () => {
  it('Funcionando?', () => {
    expect('true').to.be.an('string');
  });
});
